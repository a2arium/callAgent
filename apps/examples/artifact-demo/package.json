{
  "name": "@examples/artifact-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && cp agent.json dist/ && cp child-agent.json dist/",
    "clean": "rm -rf dist",
    "demo": "yarn build && node dist/run-demo.js"
  },
  "dependencies": {
    "@a2arium/callagent-core": "workspace:*",
    "@a2arium/callagent-memory-sql": "workspace:*",
    "@a2arium/callagent-utils": "workspace:*",
    "@prisma/client": "^6.7.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
