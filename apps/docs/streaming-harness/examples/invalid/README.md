# Invalid Fixtures

These fixtures are expected to fail validation. They are used to prove the
disposable validators catch bad stream behavior.

They are intentionally kept outside `examples/*.events.ndjson` so normal
positive validators do not load them.

