# 2.5 Observation Source/Kind Taxonomy Tweaks

## Problem
The system uses a generic `SynthesizeObservation` pattern that lacks exact topological schema boundaries laid out in the contract, occasionally drifting source formats (like child token versus agentId envelopes).

## Action Items
- Reformat `ToolEnvelope` and `ChildEnvelope` strict union payload fields to enforce the matching tracking fields strictly.
- Disambiguate and document all internal observations correctly (e.g. `internal/validation.failed`).
